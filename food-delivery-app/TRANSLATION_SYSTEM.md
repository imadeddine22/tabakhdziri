# نظام الترجمة التلقائية - Automatic Translation System

## نظرة عامة | Overview

تم تطوير نظام ترجمة تلقائي شامل يترجم جميع محتويات الموقع إلى العربية تلقائياً، بما في ذلك:
- ✅ جميع النصوص الثابتة (UI Elements)
- ✅ المنتجات الجديدة والموجودة
- ✅ الفئات (Categories)
- ✅ النماذج (Forms)
- ✅ الرسائل والإشعارات

---

## كيف يعمل النظام؟ | How It Works

### 1. **النصوص الثابتة (Static Text)**
يتم ترجمة جميع النصوص الثابتة باستخدام ملفات JSON:
- `locales/fr.json` - الترجمة الفرنسية
- `locales/ar.json` - الترجمة العربية

**مثال:**
```jsx
import { useLanguage } from '@/context/LanguageContext';

function MyComponent() {
    const { t } = useLanguage();
    
    return <h1>{t('common.home')}</h1>;
    // عند اختيار العربية: "الرئيسية"
    // عند اختيار الفرنسية: "Accueil"
}
```

### 2. **البيانات الديناميكية (Dynamic Data)**
يتم ترجمة البيانات الديناميكية (المنتجات، الفئات) تلقائياً باستخدام دالة `translateField`:

**مثال:**
```jsx
import { useLanguage } from '@/context/LanguageContext';

function ProductCard({ product }) {
    const { translateField } = useLanguage();
    
    return (
        <div>
            <h2>{translateField(product, 'name')}</h2>
            <p>{translateField(product, 'description')}</p>
        </div>
    );
}
```

**كيف تعمل `translateField`:**
- إذا كانت اللغة عربية، تبحث عن `product.name_ar`
- إذا وُجد الحقل العربي وليس فارغاً، تعيده
- وإلا تعيد `product.name` (الفرنسية)

---

## هيكل قاعدة البيانات | Database Structure

### المنتجات (Products)
```javascript
{
    name: "Couscous Royal",           // الاسم بالفرنسية (مطلوب)
    name_ar: "كسكس ملكي",             // الاسم بالعربية (اختياري)
    description: "Délicieux...",      // الوصف بالفرنسية (مطلوب)
    description_ar: "كسكس لذيذ...",   // الوصف بالعربية (اختياري)
    price: 1500,
    // ... باقي الحقول
}
```

### الفئات (Categories)
```javascript
{
    name: "Plats Traditionnels",      // الاسم بالفرنسية (مطلوب)
    name_ar: "أطباق تقليدية",         // الاسم بالعربية (اختياري)
    description: "...",               // الوصف بالفرنسية
    description_ar: "...",            // الوصف بالعربية
    // ... باقي الحقول
}
```

---

## إضافة منتج جديد | Adding New Products

### من لوحة الإدارة (Admin Panel)

عند إضافة منتج جديد، يجب ملء:
1. **الاسم** (name) - بالفرنسية
2. **الاسم بالعربية** (name_ar) - اختياري
3. **الوصف** (description) - بالفرنسية
4. **الوصف بالعربية** (description_ar) - اختياري

**ملاحظة:** إذا لم تملأ الحقول العربية، سيظهر النص الفرنسي حتى عند اختيار اللغة العربية.

### من API مباشرة

```javascript
const newProduct = {
    name: "Tajine aux légumes",
    name_ar: "طاجين بالخضار",
    description: "Un délicieux tajine...",
    description_ar: "طاجين لذيذ بالخضار...",
    price: 1200,
    category: categoryId,
    stock: 50
};

await productsAPI.create(newProduct);
```

---

## تحديث لوحة الإدارة | Updating Admin Panel

لإضافة حقول الترجمة العربية في نموذج إضافة/تعديل المنتج:

```jsx
<div>
    <label>اسم المنتج (بالفرنسية)</label>
    <input name="name" required />
</div>

<div>
    <label>اسم المنتج (بالعربية)</label>
    <input name="name_ar" />
</div>

<div>
    <label>الوصف (بالفرنسية)</label>
    <textarea name="description" required />
</div>

<div>
    <label>الوصف (بالعربية)</label>
    <textarea name="description_ar" />
</div>
```

---

## المكونات المحدثة | Updated Components

### ✅ تم تحديثها:
- `context/LanguageContext.tsx` - إضافة دالة `translateField`
- `components/RestaurantCard.jsx` - استخدام الترجمة التلقائية
- `app/product/[id]/page.tsx` - صفحة تفاصيل المنتج
- `models/Product.js` - إضافة حقول `name_ar` و `description_ar`
- `models/Category.js` - إضافة حقول `name_ar` و `description_ar`

### 📝 يحتاج تحديث (إذا لزم الأمر):
- `app/admin/products/page.tsx` - نموذج إضافة/تعديل المنتج
- `app/admin/categories/page.tsx` - نموذج إضافة/تعديل الفئة
- أي مكون آخر يعرض اسم أو وصف المنتج/الفئة

---

## أمثلة الاستخدام | Usage Examples

### مثال 1: عرض اسم المنتج
```jsx
const { translateField } = useLanguage();

// ❌ خطأ - لن يترجم
<h1>{product.name}</h1>

// ✅ صحيح - سيترجم تلقائياً
<h1>{translateField(product, 'name')}</h1>
```

### مثال 2: عرض وصف الفئة
```jsx
const { translateField } = useLanguage();

// ✅ صحيح
<p>{translateField(category, 'description')}</p>
```

### مثال 3: عرض نص ثابت
```jsx
const { t } = useLanguage();

// ✅ صحيح
<button>{t('common.save')}</button>
<p>{t('products.addToCart')}</p>
```

---

## الترجمات المتاحة | Available Translations

جميع الترجمات موجودة في `locales/ar.json`:

```json
{
    "common": {
        "home": "الرئيسية",
        "about": "من نحن",
        "contact": "اتصل بنا",
        ...
    },
    "products": {
        "title": "منتجاتنا",
        "addToCart": "إضافة إلى السلة",
        "price": "السعر",
        ...
    },
    ...
}
```

---

## نصائح مهمة | Important Tips

1. **دائماً استخدم `translateField` للبيانات الديناميكية**
   - المنتجات، الفئات، أي بيانات من قاعدة البيانات

2. **استخدم `t()` للنصوص الثابتة**
   - الأزرار، العناوين، الرسائل

3. **املأ الحقول العربية عند إضافة منتجات جديدة**
   - لضمان ظهور الترجمة الصحيحة

4. **تحقق من اتجاه النص (RTL/LTR)**
   - يتم التبديل تلقائياً عند تغيير اللغة

---

## الخلاصة | Summary

✅ **النظام الآن يدعم:**
- ترجمة تلقائية كاملة للموقع
- ترجمة المنتجات والفئات الجديدة
- ترجمة جميع النماذج والرسائل
- تبديل تلقائي بين RTL و LTR

✅ **ما عليك فعله:**
- عند إضافة منتج جديد، املأ الحقول العربية (name_ar, description_ar)
- استخدم `translateField` في أي مكون يعرض بيانات ديناميكية
- استخدم `t()` للنصوص الثابتة

---

**تم التطوير بواسطة:** Antigravity AI
**التاريخ:** 2026-01-06
