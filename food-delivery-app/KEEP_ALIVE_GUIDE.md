# 🔔 دليل استخدام Keep-Alive Script

## ما هو Keep-Alive Script؟

سكريبت بسيط يرسل طلبات دورية للخادم كل 10 دقائق لمنعه من الدخول في وضع السكون.

## 🎯 الهدف

منع الخادم المجاني على Render من الدخول في وضع السكون، مما يحسن سرعة الاستجابة للمستخدمين.

## 📋 طرق الاستخدام

### الطريقة 1: تشغيل محلي (على جهازك)

#### الخطوات:

1. **افتح Terminal في مجلد المشروع**:
   ```bash
   cd "c:\Users\DELL\Desktop\tabakh dziri\food-delivery-app"
   ```

2. **شغّل السكريبت**:
   ```bash
   node keep-alive.js
   ```

3. **اترك Terminal مفتوحاً**:
   - السكريبت سيعمل في الخلفية
   - سيرسل طلب كل 10 دقائق
   - لإيقافه: اضغط `Ctrl+C`

#### المخرجات المتوقعة:

```
🔔 Keep-Alive Script Started
📡 Backend URL: https://tabakhdziriback.onrender.com
⏰ Ping Interval: 10 minutes
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[12/01/2026 21:15:30] 📤 Pinging backend...
[12/01/2026 21:15:32] ✅ Backend is alive! (1234ms)
[12/01/2026 21:15:32] 📊 Status: OK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### الطريقة 2: استخدام خدمة Cron مجانية (مُوصى بها)

#### الخطوات:

1. **اذهب إلى [cron-job.org](https://cron-job.org)**

2. **أنشئ حساب مجاني**

3. **أنشئ Cron Job جديد**:
   - **Title**: Tabakh Dziri Keep-Alive
   - **URL**: `https://tabakhdziriback.onrender.com/api/health`
   - **Schedule**: Every 10 minutes
   - **Method**: GET

4. **احفظ وفعّل**

#### المزايا:
- ✅ لا يحتاج جهازك أن يكون مفتوحاً
- ✅ يعمل 24/7 تلقائياً
- ✅ مجاني تماماً
- ✅ موثوق

### الطريقة 3: استخدام GitHub Actions (للمطورين)

#### الخطوات:

1. **أنشئ ملف** `.github/workflows/keep-alive.yml`:

```yaml
name: Keep Backend Alive

on:
  schedule:
    # Run every 10 minutes
    - cron: '*/10 * * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend
        run: |
          curl -f https://tabakhdziriback.onrender.com/api/health || exit 1
```

2. **ارفع الملف إلى GitHub**

3. **GitHub سيشغل الـ workflow تلقائياً كل 10 دقائق**

## ⚙️ تخصيص السكريبت

### تغيير رابط الـ Backend:

```bash
# في Windows PowerShell:
$env:BACKEND_URL="https://your-backend-url.com"
node keep-alive.js

# في Linux/Mac:
BACKEND_URL="https://your-backend-url.com" node keep-alive.js
```

### تغيير الفاصل الزمني:

عدّل السطر في `keep-alive.js`:

```javascript
// من:
const PING_INTERVAL = 10 * 60 * 1000; // 10 minutes

// إلى (مثلاً 5 دقائق):
const PING_INTERVAL = 5 * 60 * 1000; // 5 minutes
```

## 📊 المراقبة

### كيف تعرف أن السكريبت يعمل؟

1. **في Terminal**:
   - ستظهر رسالة كل 10 دقائق
   - `✅ Backend is alive!` = يعمل بنجاح
   - `❌ Failed to ping` = هناك مشكلة

2. **في Render Dashboard**:
   - افتح [Render Dashboard](https://dashboard.render.com)
   - اذهب إلى خدمة الـ backend
   - تحقق من الـ Logs
   - ستجد طلبات GET إلى `/api/health` كل 10 دقائق

## ❓ أسئلة شائعة

### Q: هل يجب أن يعمل السكريبت 24/7؟
**A:** نعم، للحصول على أفضل أداء. استخدم cron-job.org بدلاً من جهازك.

### Q: هل هذا يستهلك موارد Render؟
**A:** لا، طلب `/api/health` خفيف جداً ولا يستهلك موارد.

### Q: ماذا لو توقف السكريبت؟
**A:** الخادم سيعود لوضع السكون بعد 15 دقيقة من عدم الاستخدام.

### Q: هل هناك بديل أفضل؟
**A:** نعم، الترقية إلى Render Paid Plan تزيل المشكلة تماماً.

## 🚀 التوصية

**للاستخدام الشخصي/التطوير**: استخدم الطريقة 1 (تشغيل محلي)

**للإنتاج/الاستخدام الحقيقي**: استخدم الطريقة 2 (cron-job.org)

**للمشاريع على GitHub**: استخدم الطريقة 3 (GitHub Actions)

## 📝 ملاحظات

- ⚠️ Render Free Tier لديه حد 750 ساعة/شهر
- ⚠️ Keep-Alive يستهلك من هذه الساعات
- ✅ عادةً كافٍ للمشاريع الصغيرة
- 💡 للمشاريع الكبيرة، فكر في الترقية

---

**تم الإنشاء**: 2026-01-12  
**الحالة**: ✅ جاهز للاستخدام
